---
export const prerender = false

import Layout from '@/layouts/Layout.astro'
import OnboardingForm from '@/components/editor/OnboardingForm'
---

<Layout
  title="初次設定 · Latent"
  description="建立你的 Latent 身份"
  bodyClass="bg-bg"
>
  <OnboardingForm client:only="react" />
</Layout>
