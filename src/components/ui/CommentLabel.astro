---
// src/components/ui/InputLabel.astro
import { cn } from "@/lib/utils";

interface Props {
  text: string;
  class?: string;
}

const { text, class: className } = Astro.props;
---

<div class={cn("font-mono text-sm uppercase text-ink-dim/90 mb-2 tracking-tighter", className)}>
  // {text}
</div>